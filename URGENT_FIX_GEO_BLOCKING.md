# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Географическая блокировка сервера

## ❌ Проблема
Пользователи не могут войти в приложение из-за блокировки IP адреса `37.252.20.170` в некоторых регионах.

## ✅ Решение
Настроить Cloudflare прокси для backend API (аналогично тому, как это уже сделано для Age Bot API).

---

## 📋 План действий (в указанном порядке!)

### ✅ Шаг 1: Конфигурация обновлена
- [x] Изменен API URL в `web/src/config/api.ts` на `https://backend.seplitza.ru`
- [x] Изменен API URL в `web/next.config.js` на `https://backend.seplitza.ru`
- [x] Создана инструкция `Backend-rejuvena/CLOUDFLARE_BACKEND_SETUP.md`
- [x] Обновлен `DEPLOYMENT_GUIDE_FOR_TEAM.md`

### 🔧 Шаг 2: Настроить Cloudflare DNS (СДЕЛАТЬ СЕЙЧАС!)

**Следуйте инструкции**: [Backend-rejuvena/CLOUDFLARE_BACKEND_SETUP.md](./Backend-rejuvena/CLOUDFLARE_BACKEND_SETUP.md)

Краткая версия:
1. Войти в Cloudflare Dashboard (https://dash.cloudflare.com)
2. Выбрать домен `seplitza.ru`
3. Добавить DNS запись:
   - Type: `A`
   - Name: `backend`
   - IP: `37.252.20.170`
   - **Proxy**: ✅ Включить оранжевое облако 🟠
4. Сохранить

### 🖥️ Шаг 3: Настроить Nginx на сервере (СДЕЛАТЬ СЕЙЧАС!)

**SSH подключение**:
```bash
ssh root@37.252.20.170
```

**Создать конфигурацию**:
```bash
sudo nano /etc/nginx/sites-available/backend-api
```

**Вставить** (полная конфигурация в CLOUDFLARE_BACKEND_SETUP.md):
```nginx
server {
    listen 80;
    server_name backend.seplitza.ru;
    client_max_body_size 50M;

    location / {
        proxy_pass http://localhost:9527;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
    }
}
```

**Активировать**:
```bash
sudo ln -s /etc/nginx/sites-available/backend-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 🧪 Шаг 4: Проверить работоспособность (ОБЯЗАТЕЛЬНО!)

Подождать 2-5 минут для распространения DNS, затем проверить:

```bash
# Проверка DNS (должен показать IP Cloudflare: 172.67.x.x или 104.21.x.x)
dig backend.seplitza.ru A +short

# Проверка API
curl -I https://backend.seplitza.ru/health
```

**В браузере** открыть:
- https://backend.seplitza.ru/health
- https://backend.seplitza.ru/api/exercises/public

**Ожидаемый результат**: Должны вернуться данные от API (не ошибка 404 или 502).

### 🚀 Шаг 5: Деплой frontend (ТОЛЬКО ПОСЛЕ шага 4!)

**⚠️ НЕ ДЕПЛОИТЬ ДО ТЕХ ПОР, ПОКА backend.seplitza.ru НЕ РАБОТАЕТ!**

После успешной проверки шага 4:

```bash
cd /Users/alexeipinaev/Documents/Rejuvena/web

# Коммит изменений
git add .
git commit -m "Fix: Использование Cloudflare прокси для обхода гео-блокировок (backend.seplitza.ru)"
git push origin main

# Деплой на GitHub Pages
npm run deploy
```

Подождать 2-3 минуты для распространения на GitHub Pages CDN.

---

## 🔍 Диагностика проблем

### ❌ backend.seplitza.ru не резолвится в DNS
```bash
# Проверить nameservers домена
dig seplitza.ru NS

# Должны быть cloudflare.com серверы
# Если нет - DNS запись не добавлена или не активна
```

**Решение**: Вернуться к шагу 2, убедиться что DNS запись добавлена с Proxy: ON (🟠).

### ❌ 502 Bad Gateway
```bash
# Проверить работу backend API на сервере
ssh root@37.252.20.170
pm2 status
pm2 logs rejuvena-backend --lines 20

# Проверить Nginx
sudo nginx -t
sudo systemctl status nginx
```

**Решение**: Убедиться что PM2 процесс работает на localhost:9527, и Nginx корректно проксирует.

### ❌ SSL/Certificate ошибки
- В Cloudflare Dashboard → **SSL/TLS** → Режим должен быть **Flexible**
- Подождать 10-15 минут для выпуска SSL сертификата

---

## 📊 Что будет после исправления?

### До исправления:
```
Пользователь → 37.252.20.170:9527 (прямое подключение)
                ❌ Блокируется по гео-IP в некоторых регионах
```

### После исправления:
```
Пользователь → Cloudflare CDN → backend.seplitza.ru → 37.252.20.170:9527
               ✅ Работает из любого региона
               ✅ Бесплатный SSL
               ✅ DDoS защита
               ✅ Скрытие реального IP сервера
```

---

## 📝 Примечания

- **Age Bot API** уже использует Cloudflare прокси (api.seplitza.ru) ✅
- **Backend API** будет использовать Cloudflare прокси (backend.seplitza.ru) 🔧
- **Старые URL** (api-rejuvena.duckdns.org, Azure) больше не используются 🗑️
- **DuckDNS** можно удалить после успешного переезда на Cloudflare

---

## 🆘 Если что-то пошло не так

1. **НЕ деплоить frontend** пока backend.seplitza.ru не работает!
2. Откатить изменения в web/src/config/api.ts на старый URL (api-rejuvena.duckdns.org) временно
3. Пересмотреть инструкцию [CLOUDFLARE_BACKEND_SETUP.md](./Backend-rejuvena/CLOUDFLARE_BACKEND_SETUP.md)
4. Написать в поддержку: https://t.me/seplitza_support

---

## ✅ Чеклист выполнения

- [ ] Шаг 1: Конфигурация обновлена (уже сделано ✅)
- [ ] Шаг 2: DNS запись добавлена в Cloudflare
- [ ] Шаг 3: Nginx настроен на сервере
- [ ] Шаг 4: backend.seplitza.ru работает (проверено в браузере)
- [ ] Шаг 5: Frontend задеплоен на GitHub Pages
- [ ] Проверка: Пользователи могут войти в приложение ✅
