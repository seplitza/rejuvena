# Marathon Migration Guide

## 🎯 Цель
Автоматическая миграция всех 10 марафонов из старого Azure API в новую систему.

## 📋 Текущий статус
- ✅ Найден марафон в новой системе: "Тестовый марафон оплаты" (ID: `696fab9cd2a8c56f62ebdb09`)
- ✅ Настроена миграция для "Омолодись" (14 дней)
- ⏳ Нужны Azure ID для остальных 9 марафонов

## 🚀 Использование

### 1. Просмотр списка марафонов
```bash
npx ts-node src/scripts/migrate-marathons.ts list-marathons
```

### 2. Только скачивание (без загрузки в новую систему)
```bash
npx ts-node src/scripts/migrate-marathons.ts download-only
```

### 3. Полная миграция (скачать + загрузить)
```bash
npx ts-node src/scripts/migrate-marathons.ts
```

## 📝 Подготовка к миграции

### Шаг 1: Получить Azure ID всех марафонов

1. Открой старое приложение: https://seplitza.github.io/Rejuvena_old_app/
2. Залогинься: seplitza@gmail.com / 1234
3. Открой DevTools → Network tab
4. Перейди на страницу со списком марафонов
5. Кликни на каждый марафон по очереди
6. Найди запрос: `startmarathon?marathonId=XXX`
7. Скопируй `marathonId` из URL или из Response

Запиши все ID в `marathon-config.json`.

### Шаг 2: Создать марафоны в новой админке

Для каждого марафона:
1. Открой новую админку: http://37.252.20.170:9527/admin/
2. Создай новый марафон с нужным названием и количеством дней
3. Скопируй MongoDB `_id` нового марафона
4. Добавь маппинг в `migrate-marathons.ts`:
   ```typescript
   const MARATHON_ID_MAPPING: Record<string, string> = {
     'azure-id-from-step1': 'mongodb-id-from-step2',
     // ...
   };
   ```

### Шаг 3: Обновить список марафонов

В файле `migrate-marathons.ts` обнови массив `AZURE_MARATHONS`:
```typescript
const AZURE_MARATHONS = [
  ['3842e63f-b125-447d-94a1-b1c93be38b4e', 'Омолодись', 14],
  ['azure-id-2', 'Успокойся', 20],
  ['azure-id-3', 'Эксперт молодости', 15],
  // ... остальные марафоны
] as const;
```

## 📂 Структура данных

### Скачанные данные сохраняются в:
```
marathon-migration-data/
├── 3842e63f-b125-447d-94a1-b1c93be38b4e_Омолодись.json
├── azure-id-2_Успокойся.json
└── ...
```

### Формат сохраненных данных:
```json
{
  "marathonId": "3842e63f-...",
  "title": "Омолодись",
  "subTitle": "базовый курс",
  "learningDays": [
    {
      "dayNumber": 1,
      "dayId": "azure-day-id",
      "rawData": { /* raw Azure response */ },
      "transformedData": { /* transformed for new API */ }
    }
  ]
}
```

## 🔧 API Endpoints

### Старый Azure API
- **Auth**: `POST /api/token/auth`
- **Marathon structure**: `GET /api/usermarathon/startmarathon?marathonId=XXX`
- **Day exercises**: `GET /api/usermarathon/getdayexercise?dayId=XXX`

### Новый API
- **Auth**: `POST /api/auth/login`
- **Create day**: `POST /api/marathons/admin/:marathonId/days`
- **Update day**: `PUT /api/marathons/admin/:marathonId/days/:dayId`
- **Delete day**: `DELETE /api/marathons/admin/:marathonId/days/:dayId`

## 🎨 Формат данных дня

### Что отправляется в новый API:
```json
{
  "dayNumber": 1,
  "welcomeMessage": "<p>HTML приветствие...</p>",
  "exercises": [
    {
      "categoryName": "Лимфодренажные упражнения",
      "exerciseName": "Лимфодренажные прыжки",
      "exerciseDescription": "<p>Описание...</p>",
      "marathonExerciseName": "30-100 прыжков",
      "order": 1,
      "categoryOrder": 5,
      "media": [
        {
          "type": "image",
          "url": "https://...",
          "order": 1
        },
        {
          "type": "video",
          "url": "https://player.vimeo.com/...",
          "order": 2
        }
      ]
    }
  ]
}
```

## ⚠️ Важные замечания

1. **Дни практики НЕ мигрируются** - только дни обучения (learning days)
2. **Медиа НЕ копируется** - остаются ссылки на Azure
3. **Категории сохраняются** - порядок упражнений по категориям
4. **HTML контент** переносится как есть (TipTap-совместимый)
5. **Задержки между запросами** - 500ms между днями, 2s между марафонами

## 🐛 Troubleshooting

### Ошибка: "Marathon not found"
Проверь, что `newMarathonId` существует в базе:
```bash
node -e "const mongoose = require('mongoose'); ..."
```

### Ошибка: "Unauthorized"
Проверь credentials в начале файла `migrate-marathons.ts`

### День не создался
- Проверь формат данных в логах
- Посмотри ошибку в консоли
- Проверь, что `dayNumber` уникален для марафона

## 📊 Пример запуска

```bash
# 1. Сначала только скачай данные
npx ts-node src/scripts/migrate-marathons.ts download-only

# 2. Проверь скачанные файлы
ls -la marathon-migration-data/

# 3. Если всё ок, запусти полную миграцию
npx ts-node src/scripts/migrate-marathons.ts
```

## 📈 План действий

- [ ] Получить Azure ID всех 10 марафонов
- [ ] Создать 10 марафонов в новой админке
- [ ] Обновить `MARATHON_ID_MAPPING`
- [ ] Обновить `AZURE_MARATHONS`
- [ ] Запустить `download-only` для тестирования
- [ ] Проверить скачанные данные
- [ ] Запустить полную миграцию
- [ ] Проверить результат в новой админке
