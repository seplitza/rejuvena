# 🚀 Деплой Админ-панели на Timeweb

## Быстрый деплой

```bash
cd /Users/alexeipinaev/Documents/Rejuvena/Backend-rejuvena

# 1. Сделать деплой-скрипт исполняемым
chmod +x deploy-admin.sh

# 2. Запустить деплой
./deploy-admin.sh
```

## Что происходит автоматически

1. **Билдится админка** → `admin-panel/dist/`
2. **Загружается на сервер** → `/var/www/rejuvena-backend/admin-panel/`
3. Остается только обновить nginx

## После деплоя - обновить nginx

```bash
# Подключиться к серверу
ssh root@37.252.20.170

# Скопировать новый конфиг nginx
cd /var/www/rejuvena-backend
nano /etc/nginx/sites-available/rejuvena

# Вставить содержимое файла nginx.conf из проекта
# (см. Backend-rejuvena/nginx.conf)

# Проверить конфиг
nginx -t

# Перезапустить nginx
systemctl restart nginx
```

## URL админки

**После деплоя админка будет доступна:**
- 🌐 https://api-rejuvena.duckdns.org/admin

**Логин:**
- Email: seplitza@gmail.com
- Password: 1234back

## Структура на сервере

```
/var/www/rejuvena-backend/
├── dist/              # Backend API (Node.js)
├── admin-panel/       # Статичная админка (React build)
├── uploads/           # Загруженные файлы
├── logs/              # PM2 логи
├── node_modules/
├── package.json
└── .env
```

## Nginx конфигурация

- `/admin` → статичная админка
- `/api` → backend API (proxy на :9527)
- `/uploads` → загруженные медиа
- `/` → редирект на `/admin/`

## Повторный деплой

Просто запустите снова:
```bash
./deploy-admin.sh
```

Nginx перезапускать не нужно - статика обновится автоматически.

## Локальная разработка

```bash
# Backend
npm run dev

# В новом терминале - админка
cd admin-panel
npm run dev
```

URL: http://localhost:5173
