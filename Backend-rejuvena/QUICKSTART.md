# 🚀 Быстрый старт Rejuvena Backend

## ✅ Что создано

1. **Backend API** (Node.js + Express + TypeScript)
   - Аутентификация с JWT
   - CRUD для упражнений
   - Загрузка медиафайлов
   - Система тегов
   
2. **Admin Panel** (React + TypeScript + Vite)
   - Страница логина
   - Dashboard с статистикой
   - Список упражнений
   - Редактор с TipTap
   - Drag & Drop для медиа карусели
   - Управление тегами

## 📋 Требования

- Node.js 18+ (у вас 18.20.8 - подходит)
- MongoDB 5+
- npm или yarn

## 🔧 Установка и запуск

### 1. MongoDB

Запустите MongoDB одним из способов:

**Вариант A: Homebrew (macOS)**
```bash
brew services start mongodb-community
```

**Вариант B: Docker**
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

**Вариант C: MongoDB Atlas (облако)**
- Создайте бесплатный кластер на https://www.mongodb.com/cloud/atlas
- Обновите `MONGODB_URI` в `.env`

### 2. Backend

```bash
# Установить зависимости (уже сделано)
npm install

# Инициализировать базу данных (создаст superadmin)
npm run seed

# Запустить в режиме разработки
npm run dev
```

Backend будет доступен на **http://localhost:5000**

### 3. Admin Panel

В новом терминале:

```bash
cd admin-panel

# Установить зависимости (уже сделано)
npm install

# Запустить
npm run dev
```

Admin Panel будет доступен на **http://localhost:5173**

### 4. Вход в систему

Откройте http://localhost:5173 и войдите:

- **Email:** seplitza@gmail.com
- **Password:** 1234back

## 🎯 Быстрый запуск (одной командой)

```bash
./start.sh
```

Этот скрипт:
1. Проверит MongoDB
2. Инициализирует БД
3. Запустит backend и admin panel

## 📁 Структура проекта

```
Backend-rejuvena/
├── src/                    # Backend код
│   ├── models/            # MongoDB модели (User, Exercise, Tag)
│   ├── routes/            # API endpoints
│   ├── middleware/        # Аутентификация
│   └── server.ts          # Точка входа
├── admin-panel/           # React админ-панель
│   └── src/
│       ├── pages/         # Страницы (Login, Dashboard, Exercises)
│       ├── components/    # Компоненты (Layout, TipTapEditor)
│       └── api/           # API клиент
├── uploads/               # Загруженные медиафайлы
└── .env                   # Конфигурация
```

## 🔑 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Логин
- `GET /api/auth/me` - Текущий пользователь

### Упражнения
- `GET /api/exercises` - Список всех упражнений
- `POST /api/exercises` - Создать упражнение
- `GET /api/exercises/:id` - Получить упражнение
- `PUT /api/exercises/:id` - Обновить упражнение
- `DELETE /api/exercises/:id` - Удалить упражнение

### Медиа
- `POST /api/media/upload` - Загрузить файл
- `POST /api/media/upload-url` - Добавить по URL

### Теги
- `GET /api/tags` - Список тегов
- `POST /api/tags` - Создать тег

## 🎨 Как использовать

1. **Войдите** с учетными данными superadmin
2. **Создайте теги** для категоризации (например: "Йога", "Начинающий")
3. **Создайте упражнение:**
   - Заполните название и описание
   - Используйте TipTap редактор для контента
   - Загрузите изображения для карусели
   - Перетащите изображения для изменения порядка
   - Добавьте теги
   - Опубликуйте или сохраните как черновик

## 🐛 Решение проблем

### MongoDB не подключается
```bash
# Проверьте, запущен ли MongoDB
pgrep -x mongod

# Если нет, запустите
brew services start mongodb-community
```

### Порт 5000 занят
Измените `PORT` в `.env`

### Ошибка при установке зависимостей
```bash
# Очистите кэш
rm -rf node_modules package-lock.json
rm -rf admin-panel/node_modules admin-panel/package-lock.json
npm install
cd admin-panel && npm install
```

### Backend не видит изменения
```bash
# Перезапустите с очисткой
npm run dev
```

## 📚 Документация

- [SETUP.md](SETUP.md) - Подробная документация по проекту
- [DEPLOYMENT.md](DEPLOYMENT.md) - Инструкции по развертыванию

## 🚀 Следующие шаги

1. **Создайте GitHub репозиторий**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/seplitza/backend-rejuvena.git
   git push -u origin main
   ```

2. **Разверните на продакшн** (см. [DEPLOYMENT.md](DEPLOYMENT.md))
   - Backend → Railway/Render/Heroku
   - Admin Panel → GitHub Pages/Vercel
   - Database → MongoDB Atlas

3. **Интегрируйте с фронтенд приложением Rejuvena**
   ```javascript
   // В вашем основном приложении Rejuvena
   const exercises = await fetch('https://your-backend-url.com/api/exercises')
     .then(res => res.json())
     .then(data => data.filter(e => e.isPublished));
   ```

## 💡 Советы

- Используйте TipTap редактор для вставки изображений прямо в контент
- Карусель изображений показывается отдельно от контента
- Drag & Drop работает как для загрузки, так и для изменения порядка
- Теги можно создавать на лету при редактировании упражнения
- Черновики не будут видны в основном приложении Rejuvena

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте, что MongoDB запущен
2. Убедитесь, что порты 5000 и 5173 свободны
3. Проверьте логи в консоли
4. Убедитесь, что `.env` настроен правильно

---

**Готово! 🎉 Ваш бэкенд и админ-панель для Rejuvena готовы к использованию.**

Начните с запуска MongoDB, затем выполните `npm run seed` и `npm run dev` в корневой папке, и `npm run dev` в папке admin-panel.
