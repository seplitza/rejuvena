# Улучшения UX регистрации и входа

## ✅ Что сделано

### 1. Бэкенд (Backend-rejuvena)
- ✅ Изменен генератор паролей: **4 цифры** вместо 8 символов
- ✅ Пример: `1234`, `5678`, `9012`
- ✅ Email шаблон уже на русском языке
- ✅ Deployed на сервер (PM2 restart #51)

**Файлы:**
- `src/services/email.service.ts` - generatePassword() теперь возвращает 4 цифры
- `src/routes/auth.routes.ts` - убран параметр (8) при вызове

### 2. Фронтенд (web/)
Создан улучшенный компонент: `docs/frontend/signup-improved.tsx`

**Новые возможности:**
- ✅ Русский язык по умолчанию
- ✅ Переключатель RU/EN в правом верхнем углу (фикс флаг + текст)
- ✅ Все тексты переводятся на выбранный язык
- ✅ Модальное окно после успешной регистрации с:
  - Зеленая галочка
  - Заголовок "Регистрация прошла успешно!"
  - Текст: "Проверьте свой email и используйте временный пароль для входа"
  - Предупреждение: "Обязательно смените пароль в ЛК"
  - Кнопка "Перейти на страницу входа"

## 📝 Как применить на фронтенде

### Шаг 1: Скопировать улучшенный компонент
```bash
cd /Users/alexeipinaev/Documents/Rejuvena/web
cp ../Backend-rejuvena/docs/frontend/signup-improved.tsx src/pages/auth/signup.tsx
```

### Шаг 2: Добавить язык в localStorage (опционально)
Если хочешь сохранять выбор языка между страницами, добавь в компонент:

```typescript
// В начале компонента
const [language, setLanguage] = useState<'ru' | 'en'>(() => {
  if (typeof window !== 'undefined') {
    return (localStorage.getItem('language') as 'ru' | 'en') || 'ru';
  }
  return 'ru';
});

// При смене языка
const toggleLanguage = () => {
  const newLang = language === 'ru' ? 'en' : 'ru';
  setLanguage(newLang);
  if (typeof window !== 'undefined') {
    localStorage.setItem('language', newLang);
  }
};
```

### Шаг 3: Аналогично обновить страницу логина
Создай похожий компонент для `/auth/login` с переключателем языков.

### Шаг 4: Обновить Redux action
Убедись что `signupWithEmail` возвращает правильный статус:

```typescript
// В src/store/modules/auth/slice.ts
export const signupWithEmail = createAsyncThunk(
  'auth/signupWithEmail',
  async (data: { email: string; firstName: string; lastName: string }) => {
    const response = await fetch('https://api-rejuvena.duckdns.org/user/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    
    if (!response.ok) {
      const error = await response.json();
      throw new Error(error.message || 'Registration failed');
    }
    
    return await response.json();
  }
);
```

## 🎨 Дизайн модального окна

```
┌────────────────────────────────────┐
│                                    │
│         [✓ Зеленый кружок]        │
│                                    │
│   Регистрация прошла успешно!     │
│                                    │
│   Проверьте свой email и          │
│   используйте временный пароль    │
│   для входа.                      │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ ⚠️ Обязательно смените пароль│ │
│  │ на собственный в личном      │ │
│  │ кабинете.                    │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌────────────────────────────┐   │
│  │ Перейти на страницу входа  │   │
│  └────────────────────────────┘   │
│                                    │
└────────────────────────────────────┘
```

## 🧪 Тестирование

### Проверь бэкенд (4-значный пароль):
```bash
# Удали тестового пользователя если есть
ssh root@37.252.20.170 "mongosh rejuvena --eval 'db.users.deleteOne({email: \"test@test.com\"})'"

# Зарегистрируйся
curl -X POST https://api-rejuvena.duckdns.org/user/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","firstName":"Test","lastName":"User"}'

# Проверь email - должен прийти пароль из 4 цифр (например: 5421)
```

### Проверь фронтенд:
1. Открой https://seplitza.github.io/rejuvena/auth/signup
2. Проверь что язык по умолчанию - русский
3. Переключи на английский - все должно перевестись
4. Заполни форму и отправь
5. Должно появиться модальное окно с инструкциями
6. Кликни "Перейти на страницу входа" - должен редирект на /auth/login

## 📦 Деплой фронтенда

```bash
cd /Users/alexeipinaev/Documents/Rejuvena/web
git add src/pages/auth/signup.tsx
git commit -m "feat: Improve signup UX - Russian by default, 4-digit password, success modal"
git push
```

GitHub Actions автоматически задеплоит на GitHub Pages.

## 🔄 Следующие шаги

1. ✅ Бэкенд готов (4 цифры)
2. 🔲 Скопировать signup-improved.tsx в web/
3. 🔲 Аналогично обновить login.tsx (добавить переключатель языков)
4. 🔲 Добавить язык в контекст/localStorage для всех страниц
5. 🔲 Обновить страницу смены пароля в ЛК
6. 🔲 Протестировать полный флоу: регистрация → email → логин → смена пароля

## 📄 Структура переводов

Если хочешь вынести переводы в отдельный файл:

```typescript
// src/i18n/translations.ts
export const translations = {
  signup: {
    ru: {
      title: 'Создать аккаунт',
      // ... все тексты
    },
    en: {
      title: 'Create Account',
      // ... все тексты
    }
  },
  login: {
    ru: { /* ... */ },
    en: { /* ... */ }
  }
};
```

## ✅ Итого

**Бэкенд:**
- ✅ Deployed (commit 9d80e44)
- ✅ 4-значный пароль работает
- ✅ Email на русском языке

**Фронтенд:**
- ✅ Код готов в `docs/frontend/signup-improved.tsx`
- 🔲 Нужно скопировать в `web/src/pages/auth/signup.tsx`
- 🔲 Задеплоить

**Следующий шаг:** Скопируй файл и задеплой фронтенд!
