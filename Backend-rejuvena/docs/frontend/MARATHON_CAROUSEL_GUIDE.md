Ghbikj dhtvz gthtdtcnb jgkfne d ,jtdjq ht;bv# Marathon Carousel Integration Guide

## Интеграция карусели марафонов на главную страницу ЛК

### 1. Компонент MarathonCarousel.tsx

Компонент создан в `docs/frontend/components/MarathonCarousel.tsx`

**Основные возможности:**
- ✅ Автоматическая загрузка марафонов с флагом `isDisplay: true`
- ✅ Карусель с навигацией (стрелки влево/вправо)
- ✅ Индикаторы слайдов
- ✅ Адаптивный дизайн (mobile-friendly)
- ✅ Красивый градиентный фон
- ✅ Клик на карточку → переход на страницу марафона
- ✅ Показывает платный/бесплатный статус
- ✅ Форматирование даты на русском языке

### 2. Использование в приложении

```tsx
import MarathonCarousel from '@/components/MarathonCarousel';

export default function Dashboard() {
  return (
    <div className="dashboard">
      <h1>Личный кабинет</h1>
      
      {/* Карусель марафонов - размещается вместе с Premium предложением */}
      <MarathonCarousel />
      
      {/* Остальной контент */}
      <PremiumPlanCard />
      <ExerciseList />
    </div>
  );
}
```

### 3. API Endpoint

Компонент использует существующий endpoint:

**GET** `/api/marathons`

```typescript
// Возвращает марафоны с условиями:
// - isPublic: true
// - isDisplay: true (Показывать на витрине)
// - Сортировка: по дате старта (новые первыми)
// - Исключает: welcomeMessage, courseDescription, rules
```

**Пример ответа:**
```json
{
  "success": true,
  "marathons": [
    {
      "_id": "69733e6cf22ce2297694b8a3",
      "title": "Look younger",
      "description": "14-дневный марафон омоложения",
      "numberOfDays": 14,
      "cost": 2990,
      "isPaid": true,
      "isPublic": true,
      "isDisplay": true,
      "language": "en",
      "startDate": "2026-02-01T00:00:00.000Z"
    }
  ]
}
```

### 4. Настройка марафона для витрины

В админ-панели при редактировании марафона:

1. Откройте марафон на редактирование
2. Вкладка "Информация"
3. Установите чекбокс **"Показывать на витрине"** (`isDisplay`)
4. Убедитесь что **"Публичный доступ"** (`isPublic`) тоже включен
5. Сохраните

Только марафоны с обоими флагами появятся в карусели.

### 5. Стилизация

Компонент использует inline CSS-in-JS (`<style jsx>`), но можно переписать на:

**Tailwind CSS:**
```tsx
<div className="mx-4 my-5 p-5 bg-gradient-to-r from-purple-600 to-purple-800 rounded-2xl">
  <h2 className="text-white text-2xl font-bold mb-5 text-center">
    🏃 Марафоны
  </h2>
  {/* ... */}
</div>
```

**CSS Modules:**
```tsx
import styles from './MarathonCarousel.module.css';

<div className={styles.marathonCarousel}>
  <h2 className={styles.carouselTitle}>🏃 Марафоны</h2>
  {/* ... */}
</div>
```

### 6. Размещение на странице

**Рекомендуемая последовательность:**

```tsx
<div className="dashboard-container">
  {/* 1. Приветствие */}
  <WelcomeSection />
  
  {/* 2. Марафоны (если есть) */}
  <MarathonCarousel />
  
  {/* 3. Premium предложение */}
  <PremiumPlanCard />
  
  {/* 4. Упражнения */}
  <ExerciseList />
  
  {/* 5. Остальное */}
  <PhotoDiary />
  <ActivityFeed />
</div>
```

### 7. Функции карусели

**Автоматическая прокрутка (опционально):**
```tsx
useEffect(() => {
  if (marathons.length <= 1) return;
  
  const interval = setInterval(() => {
    nextSlide();
  }, 5000); // Каждые 5 секунд
  
  return () => clearInterval(interval);
}, [marathons.length, currentIndex]);
```

**Touch/Swipe поддержка (опционально):**
```tsx
import { useSwipeable } from 'react-swipeable';

const handlers = useSwipeable({
  onSwipedLeft: () => nextSlide(),
  onSwipedRight: () => prevSlide(),
});

<div {...handlers} className="marathon-card">
  {/* ... */}
</div>
```

### 8. Тестирование

1. Создайте несколько марафонов в админ-панели
2. Установите флаги `isPublic` и `isDisplay`
3. Откройте главную страницу ЛК
4. Проверьте:
   - ✅ Карусель отображается
   - ✅ Стрелки переключают слайды
   - ✅ Индикаторы показывают текущий слайд
   - ✅ Клик на карточку открывает страницу марафона
   - ✅ Мобильная версия корректна

### 9. Возможные улучшения

- **Фото обложки марафона** - добавить поле `coverImage` в модель Marathon
- **Категории марафонов** - фильтрация по темам (лицо, тело, питание)
- **Таймер до старта** - countdown для скорого начала
- **Прогресс пользователя** - если записан, показывать процент выполнения
- **Lazy loading** - подгрузка марафонов по мере прокрутки

### 10. Пример с Premium блоком

```tsx
export default function Dashboard() {
  const { user } = useSelector((state) => state.auth);
  
  return (
    <div className="dashboard">
      <h1>Добро пожаловать, {user?.firstName}!</h1>
      
      {/* Марафоны - редкое событие, показываем в приоритете */}
      <MarathonCarousel />
      
      {/* Premium предложение */}
      {!user?.isPremium && (
        <div className="premium-offer">
          <h2>⭐ Премиум подписка</h2>
          <p>Получите доступ ко всем упражнениям и марафонам</p>
          <button onClick={handlePremiumPurchase}>
            Купить за 990 ₽/месяц
          </button>
        </div>
      )}
      
      <ExerciseList />
    </div>
  );
}
```

---

## Быстрый старт

1. Скопируйте `MarathonCarousel.tsx` в `src/components/`
2. Импортируйте в главную страницу ЛК
3. Разместите перед Premium блоком
4. В админке включите флаг "Показывать на витрине" для нужных марафонов
5. Готово! 🎉

## Вопросы?

- Марафоны не отображаются → Проверьте флаги `isPublic` и `isDisplay` в БД
- Ошибка CORS → Убедитесь что `API_URL` правильный
- Карусель не переключается → Проверьте что марафонов больше 1
