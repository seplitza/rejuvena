# Руководство по редактору лендингов

## Обзор системы

Новый редактор лендингов работает как **Tilda**: визуальное управление блоками с drag-and-drop и редактированием в режиме предпросмотра.

## Основные возможности

### 1. Управление блоками (Section Manager)

**Расположение:** Сразу под AI-генератором в редакторе лендинга

**Функции:**
- 🔄 **Перетаскивание** - меняйте порядок блоков drag-and-drop
- 👁️ **Показать/скрыть** - зеленая кнопка для видимости блока
- ✏️ **Редактировать** - фиолетовая кнопка открывает модальное окно с редактором

**Обязательные блоки:**
- 🎯 Hero (первый экран) - всегда видим, нельзя скрыть
- 🏃 Marathons (тарифы) - всегда видим, нельзя скрыть

**Опциональные блоки:**
- ✨ Что такое система (featuresSection)
- 🎯 Проблемы которые решаем (problemsSection)
- 👤 Об авторе (aboutSection)
- 📊 Ступени системы (stepsSection)
- 🔄 Как проходит программа (processSection)
- 📈 Результаты клиентов (statsSection)

### 2. Редактирование блоков

При нажатии на **✏️ Редактировать** открывается **модальное окно** с:

1. **Формой редактирования** - изменяйте текст, иконки, изображения
2. **Живым предпросмотром** - видите результат сразу
3. **Кнопками управления**:
   - **+ Добавить** - добавить элемент (фичу, проблему, шаг)
   - **Удалить** - удалить элемент
   - **Отмена** - закрыть без сохранения
   - **Сохранить** - применить изменения

### 3. Типы блоков и их редакторы

#### ✨ Features Section (Что такое система)
- **Заголовок** + подзаголовок секции
- **4 фичи** (по умолчанию):
  - Иконка (emoji)
  - Заголовок
  - Описание
- Сетка 2-4 колонки (адаптивная)

#### 🎯 Problems Section (Проблемы)
- **Заголовок** + подзаголовок
- **4 проблемы** с номерами (01, 02, 03, 04):
  - Номер (редактируемый)
  - Заголовок
  - Описание
- Карточки с градиентными бейджами

#### 👤 About Section (Об авторе)
- **Заголовок секции**
- **Имя** автора
- **Биография** (multiline)
- **Фото** (URL)
- **Достижения** (до 3):
  - Иконка (emoji)
  - Заголовок
  - Описание

#### 📊 Steps Section (Ступени системы)
- **Заголовок** + подзаголовок
- **4 ступени**:
  - Заголовок (с номером)
  - Описание
  - Изображение (опционально, URL)

#### 🔄 Process Section (Как проходит программа)
- **Заголовок** + подзаголовок
- **5 этапов процесса**:
  - Номер (число)
  - Заголовок
  - Описание
  - Длительность (например: "1-2 дня")
- Вертикальный timeline с бейджами

#### 📈 Stats Section (Результаты клиентов)
- **Заголовок секции**
- **4 статистики**:
  - Значение (например: "-7 лет")
  - Метка (например: "Биологический возраст")
  - Описание
- Градиентный фон purple-pink-orange

## Рабочий процесс

### Создание нового лендинга

1. **Нажмите "Создать лендинг"**
2. **Используйте AI-генератор** (опционально):
   - Выберите марафон
   - Нажмите "✨ Сгенерировать"
   - Система заполнит Hero и Marathons
3. **Управление блоками**:
   - Все 6 дополнительных секций **включены по умолчанию**
   - Содержат **преднастроенный контент** из оригинального лендинга
   - Можете скрыть ненужные или изменить порядок
4. **Редактирование**:
   - Нажмите ✏️ на нужном блоке
   - Измените контент в модальном окне
   - Сохраните
5. **Финальная настройка**:
   - Заполните Hero и Marathons в основной форме
   - Проверьте slug (URL)
   - Установите чекбокс "Опубликовать сразу"
6. **Сохраните лендинг**

### Редактирование существующего

1. **Откройте лендинг** из списка
2. **Порядок блоков** восстанавливается автоматически
3. **Видимость** блоков загружается из базы
4. **Редактируйте** любой блок через модальное окно
5. **Сохраните** изменения

## Технические детали

### Drag-and-Drop
- Библиотека: `@dnd-kit/core` + `@dnd-kit/sortable`
- Работает с React 19
- Smooth анимации перемещения
- Нельзя перетащить обязательные блоки

### Предпросмотр
- Каждый редактор имеет **живой preview**
- Использует те же стили, что и фронтенд
- Tailwind CSS классы идентичны продакшену

### Сохранение
- Блоки сохраняются только если `isVisible = true`
- API: `POST /api/landings` или `PUT /api/landings/:id`
- Секции опциональные в MongoDB схеме

### Значения по умолчанию

При создании нового лендинга автоматически подставляются:
- `defaultFeatures` - 4 фичи о системе Сеплица
- `defaultProblems` - 4 возрастных признака
- `defaultAbout` - информация об Алексее Пинаеве
- `defaultSteps` - 4 ступени системы
- `defaultProcess` - 5 этапов программы
- `defaultStats` - 4 результата клиентов

**Все контент из файла:** `admin-panel/src/types/sections.ts`

## Советы по использованию

### Порядок блоков на лендинге
Рекомендуемая последовательность (можно менять):
1. Hero (первый экран) - обязательно
2. Features (что такое система)
3. Problems (какие проблемы решаем)
4. About (об авторе)
5. Steps (ступени системы)
6. Process (как проходит программа)
7. Stats (результаты клиентов)
8. Marathons (тарифы) - обязательно

### Иконки
- Используйте **emoji** для иконок
- Копируйте из [emojipedia.org](https://emojipedia.org/)
- Примеры: 🏃 💆 🧬 🦠 🎓 ⭐ 📚

### Изображения
- Используйте **абсолютные URL**
- Для загруженных: `http://37.252.20.170:9527/uploads/...`
- Для внешних: любой публичный URL

### SEO оптимизация
- Заголовки секций - **короткие и понятные**
- Описания - **до 150 символов** на элемент
- Используйте **ключевые слова**: омоложение, марафон, Сеплица

## Горячие клавиши

- **Esc** - закрыть модальное окно (без сохранения)
- **Enter** (в форме) - сохранить изменения
- **Tab** - переключение между полями

## Решение проблем

### Секция не сохраняется
✅ Проверьте что `isVisible = true` (зеленая кнопка)
✅ Все обязательные поля заполнены (title, description)

### Порядок сбросился
✅ Перетащите блоки снова и сохраните лендинг
✅ Порядок сохраняется только после submit формы

### Предпросмотр не обновляется
✅ Измените поле и кликните вне него
✅ State обновляется при onChange событии

### Модальное окно не открывается
✅ Hero и Marathons редактируются в основной форме
✅ Только 6 дополнительных секций имеют модальные окна

## API документация

### Структура данных секций

```typescript
// Features Section
{
  featuresSection: {
    sectionTitle: string;
    subtitle: string;
    features: Array<{
      icon: string;
      title: string;
      description: string;
    }>;
  }
}

// Problems Section
{
  problemsSection: {
    sectionTitle: string;
    subtitle: string;
    problems: Array<{
      number: string;
      title: string;
      description: string;
    }>;
  }
}

// About Section
{
  aboutSection: {
    sectionTitle: string;
    name: string;
    bio: string;
    photo: string;
    achievements: Array<{
      icon: string;
      title: string;
      description: string;
    }>;
  }
}

// Steps Section
{
  stepsSection: {
    sectionTitle: string;
    subtitle: string;
    steps: Array<{
      title: string;
      description: string;
      image?: string;
    }>;
  }
}

// Process Section
{
  processSection: {
    sectionTitle: string;
    subtitle: string;
    steps: Array<{
      number: number;
      title: string;
      description: string;
      duration: string;
    }>;
  }
}

// Stats Section
{
  statsSection: {
    sectionTitle: string;
    stats: Array<{
      value: string;
      label: string;
      description: string;
    }>;
  }
}
```

## Примеры использования

### Создание лендинга для нового марафона

1. Перейдите в **Лендинги** → **Создать лендинг**
2. Выберите марафон из дропдауна
3. Нажмите **✨ Сгенерировать** - заполнятся Hero и Marathons
4. Проверьте автозаполненные секции в Section Manager
5. Отредактируйте **About Section** - добавьте свою информацию
6. Скройте **Stats Section** если нет данных
7. Перетащите **Problems** перед **Features** если нужно
8. Сохраните с чекбоксом "Опубликовать сразу"

### Обновление контента существующего лендинга

1. Откройте лендинг из списка
2. Нажмите ✏️ на **Features Section**
3. Измените описание первой фичи
4. Добавьте 5-ю фичу через **+ Добавить**
5. Сохраните в модальном окне
6. Нажмите **Сохранить** внизу страницы

### Создание минимального лендинга

1. Создайте новый лендинг
2. Заполните только **Hero** и **Marathons** (обязательные)
3. В Section Manager скройте все опциональные секции (кроме Hero и Marathons)
4. Сохраните - получите лендинг только с Hero и тарифами

## Ссылки

- **Админ панель:** https://api-rejuvena.duckdns.org/admin/
- **Пример лендинга:** https://seplitza.github.io/rejuvena/landing/omolodis-stage-7-2280
- **API docs:** См. `QUICKSTART.md` в корне проекта
