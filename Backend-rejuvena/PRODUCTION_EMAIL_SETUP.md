# 🔑 Настройка RESEND_API_KEY для Production

## Быстрая инструкция

### 1. Получить API ключ Resend

**Тот же ключ можно использовать для всего!** Resend позволяет один API ключ для регистрации, паролей И марафонов.

Если у тебя уже есть ключ для логина/регистрации - используй его же! Если нет или хочешь отдельный:

#### Шаг 1: Войти в Resend
```
https://resend.com
```
- Войди через тот же аккаунт, где создавал ключ для логина
- Или создай новый аккаунт

#### Шаг 2: Создать API Key
1. Слева в меню: **API Keys**
2. Нажми **"Create API Key"**
3. Name: `rejuvena-marathons` (или `rejuvena-production`)
4. Permission: **Full Access** (или **Sending access**)
5. Нажми **Create**
6. **ВАЖНО:** Скопируй ключ сразу! Он показывается только 1 раз
   - Формат: `re_AbCdEf123456789...`

### 2. Добавить на production сервер

```bash
# Подключиться к серверу
ssh root@37.252.20.170

# Перейти в директорию backend
cd /var/www/rejuvena-backend

# Открыть .env файл
nano .env
```

#### Добавить или обновить строки:
```env
# Resend Email Service
RESEND_API_KEY=re_твой_ключ_сюда
EMAIL_FROM=noreply@rejuvena.com
```

**Или если используешь тестовый домен Resend:**
```env
RESEND_API_KEY=re_твой_ключ_сюда
EMAIL_FROM=onboarding@resend.dev
```

Сохранить: `Ctrl+X`, затем `Y`, затем `Enter`

### 3. Перезапустить сервер

```bash
# Перезапустить PM2 с новыми переменными
pm2 restart rejuvena-backend --update-env

# Перезапустить cron для уведомлений
pm2 restart ecosystem.config.json --update-env

# Проверить логи
pm2 logs rejuvena-backend --lines 20
```

**Должны увидеть:**
```
✅ Resend email service initialized
✅ Connected to MongoDB
🚀 Server running on port 9527
```

### 4. Проверить работу

#### Тест регистрации с email:
```bash
curl -X POST http://37.252.20.170:9527/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test1234"
  }'
```

Проверь почту `test@example.com` - должно прийти письмо с паролем.

#### Тест создания марафона и записи:
1. Создай марафон в админке: http://37.252.20.170:9527/admin/marathons
2. Запишись через фронт: https://seplitza.github.io/rejuvena/marathons
3. Проверь email - должно прийти подтверждение записи

---

## Частые вопросы

### Q: Нужен ли отдельный ключ для марафонов?
**A:** Нет! Один API ключ работает для всего: регистрации, паролей, марафонов.

### Q: Письма уходят в спам
**A:** Это нормально для `onboarding@resend.dev`. Чтобы исправить:
1. Настрой свой домен в Resend (см. [RESEND_SETUP.md](RESEND_SETUP.md))
2. Добавь DNS записи SPF/DKIM/DMARC
3. Используй `EMAIL_FROM=noreply@твой-домен.com`

### Q: Превышен лимит 100 писем/день
**A:** Бесплатный план Resend: 100 писем/день, 3000/месяц.
- Для production может потребоваться платный план ($20/мес = 50,000 писем)
- Или использовать другой сервис (Mailgun, SendGrid)

### Q: Как проверить отправку?
**A:** Смотри логи:
```bash
pm2 logs rejuvena-backend | grep "email"
```

Ищи строки:
- `✅ Registration email sent to ...`
- `✅ Marathon enrollment email sent to ...`
- `❌ Failed to send email` (если ошибка)

### Q: Email не приходят, но ошибок нет
**A:** Проверь Resend Dashboard:
1. https://resend.com/emails
2. Посмотри статус отправленных писем
3. Проверь Bounce/Complaint rate

---

## Быстрый чеклист

- [ ] Получил API ключ Resend (или использую существующий)
- [ ] Добавил `RESEND_API_KEY` в `/var/www/rejuvena-backend/.env`
- [ ] Добавил `EMAIL_FROM` в `.env`
- [ ] Перезапустил PM2: `pm2 restart rejuvena-backend --update-env`
- [ ] Проверил логи: увидел "Resend email service initialized"
- [ ] Протестировал регистрацию - письмо пришло
- [ ] Протестировал запись на марафон - письмо пришло
- [ ] (Опционально) Настроил свой домен для избежания спама

---

## Мониторинг email

### Посмотреть отправленные письма:
```bash
pm2 logs rejuvena-backend | grep "email sent"
```

### Посмотреть ошибки отправки:
```bash
pm2 logs rejuvena-backend | grep "Failed to send"
```

### Статистика марафонов:
```bash
mongosh rejuvena --eval "db.marathonEnrollments.countDocuments()"
```

---

## 🚀 Готово!

После настройки:
- Регистрация отправляет пароль на email
- Запись на марафон отправляет подтверждение
- За день до старта - напоминание
- В день старта - уведомление
- Каждый день - напоминание
- По завершению - поздравление

**Все email автоматические! 📧**
