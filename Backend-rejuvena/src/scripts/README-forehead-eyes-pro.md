# Импорт упражнений из курса "+на лоб и глаза"

## Описание

Скрипт импортирует упражнения из продвинутого курса "+на лоб и глаза" (Marathon ID: `11e5f1f2-de4e-4833-a7e5-3089c40be78f`).

## Категории упражнений

Скрипт импортирует упражнения из следующих категорий:

### 1. PRO на шею (2 упражнения)
- Разволокнение задней поверхности шеи
- Вращения головой с акцентом на растяжение

### 2. PRO на лоб и глаза (10 упражнений)
- Лифтинг лба, бровей и верхних век
- Лифтинг висков PRO
- Массаж межбровья
- Спиральный массаж глаз
- Лимфодренаж глаз PRO
- Граница веко-щека
- Верхнее веко PRO
- Перетирания висков
- Прокатывание складочки
- Стирание морщин на лбу PRO

## Теги

Все импортированные упражнения получают следующие теги:
- `продвинутое`
- `PRO`
- `+на_лоб_и_глаза`

## Предотвращение дубликатов

Скрипт проверяет наличие упражнений в базе данных:
- Если упражнение с таким названием уже существует **и имеет теги** → пропускается (считается дубликатом из другого курса)
- Если упражнение существует **без тегов** → обновляется с добавлением новых тегов
- Если упражнение не существует → создается новое

## Запуск

### Предварительные требования

Убедитесь, что в файле `.env` установлены:
```env
MONGODB_URI=mongodb://localhost:27017/rejuvena
OLD_API_TOKEN=your_api_token_here
```

### Команда запуска

```bash
npm run import-forehead-eyes-pro
```

или напрямую через ts-node:

```bash
ts-node src/scripts/import-forehead-eyes-pro.ts
```

## Что делает скрипт

1. **Подключается к MongoDB**
2. **Запрашивает данные** из API старого приложения
3. **Создает теги** (если их нет):
   - `продвинутое`
   - `PRO`
   - `+на_лоб_и_глаза`
4. **Обрабатывает каждое упражнение**:
   - Проверяет на дубликаты
   - Конвертирует `exerciseContents` в `carouselMedia`
   - Сохраняет описание и медиа-контент
5. **Выводит статистику**:
   - ✅ Импортировано новых
   - 🔄 Обновлено существующих
   - ⏭️ Пропущено дубликатов
   - ❌ Ошибок

## Пример вывода

```
🔌 Подключаемся к MongoDB...
✅ Подключено к MongoDB
📡 Запрашиваем данные из API...
📦 Получено категорий: 8
📂 Найдено категорий: 2

✅ Создан тег: #продвинутое
✅ Создан тег: #PRO
✅ Создан тег: #+на_лоб_и_глаза

📂 Обрабатываем категорию: PRO на шею (2 упражнений)
✅ Импортировано: Разволокнение задней поверхности шеи (2 медиа)
✅ Импортировано: Вращения головой с акцентом на растяжение (2 медиа)

📂 Обрабатываем категорию: PRO на лоб и глаза (10 упражнений)
✅ Импортировано: Лифтинг лба, бровей и верхних век (2 медиа)
⏭️ Пропущено (дубликат): Массаж межбровья
...

📊 Результаты импорта:
✅ Импортировано новых: 10
🔄 Обновлено существующих: 2
⏭️ Пропущено дубликатов: 0
❌ Ошибок: 0
📦 Всего обработано: 12

👋 Отключено от MongoDB
```

## Структура данных

### Входные данные (из API)

```typescript
{
  exerciseName: string;
  exerciseDescription: string;
  marathonExerciseName: string; // Длительность/повторения
  order: number;
  exerciseContents: Array<{
    type: 'image' | 'video';
    contentPath: string;
    order: number;
    isActive: boolean;
  }>;
}
```

### Выходные данные (в MongoDB)

```typescript
{
  title: string;
  description: string;
  content: string; // HTML описание
  carouselMedia: Array<{
    type: 'image' | 'video';
    url: string;
    filename: string;
    order: number;
  }>;
  tags: ObjectId[]; // Ссылки на теги
  duration: string;
  category: string;
  order: number;
}
```

## Устранение неполадок

### Ошибка подключения к MongoDB
```
❌ Ошибка: connect ECONNREFUSED
```
**Решение**: Убедитесь, что MongoDB запущен и `MONGODB_URI` правильно указан в `.env`

### Ошибка API
```
❌ Ошибка: Request failed with status code 401
```
**Решение**: Проверьте `OLD_API_TOKEN` в файле `.env`

### Дубликаты не пропускаются
**Решение**: Скрипт пропускает только упражнения, которые уже имеют теги. Если нужно принудительно пропустить упражнение, добавьте ему любой тег вручную.

## Связанные скрипты

- `import-exercises.ts` - Импорт базовых упражнений
- `import-cheeks-eyes-pro.ts` - Импорт PRO упражнений для щек и глаз
- `import-all-exercises-from-api.ts` - Массовый импорт всех упражнений
