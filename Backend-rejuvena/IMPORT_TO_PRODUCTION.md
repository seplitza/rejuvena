# Импорт упражнений на Production

## 📦 Импорт упражнений для шеи PRO

### Вариант 1: SSH на сервер (рекомендуется)

```bash
# 1. Подключиться к серверу
ssh root@37.252.20.170
# Пароль: c+d2Ei@GeWWKq8

# 2. Перейти в директорию проекта
cd /var/www/rejuvena-backend

# 3. Обновить код с GitHub
git pull origin main

# 4. Установить зависимости (если нужно)
npm install

# 5. Запустить импорт
npx ts-node src/scripts/import-neck-pro-manual.ts

# 6. Проверить результат
mongosh mongodb://localhost:27017/rejuvena --eval "db.exercises.countDocuments()"
# Должно быть 63 (было 57)

# 7. Выйти
exit
```

### Вариант 2: Локально с SSH туннелем

Если нужен локальный запуск с подключением к production MongoDB:

```bash
# 1. Создать SSH туннель
ssh -L 27018:localhost:27017 root@37.252.20.170 -N &

# 2. В .env добавить
PROD_MONGODB_URI=mongodb://localhost:27018/rejuvena

# 3. Запустить скрипт
./src/scripts/import-neck-pro.sh production
```

## ✅ После импорта

Проверить в админ-панели:
https://api-rejuvena.duckdns.org/admin/exercises

Должно появиться 6 новых упражнений:
- Массаж ГКСМ
- Глубокая пальпация
- Хорды
- Лифтинг диафрагмы рта
- Лифтинг второго подбородка
- Перетирание морщин на шее

Все с тегами: **Шея**, **+на шею**, **PRO**

## 🔄 Автоматизация (опционально)

Чтобы автоматически запускать импорт после деплоя, можно добавить в `.github/workflows/deploy.yml`:

```yaml
- name: Run import scripts
  run: |
    cd /var/www/rejuvena-backend
    npx ts-node src/scripts/import-neck-pro-manual.ts || true
```
