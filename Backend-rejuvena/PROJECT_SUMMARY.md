# Rejuvena Backend & Admin Panel

> Полнофункциональная система управления контентом для приложения Rejuvena

## 🎯 Что реализовано

### ✅ Backend API (Node.js + Express + TypeScript)
- **Аутентификация** с JWT токенами
- **CRUD операции** для упражнений
- **Загрузка медиа** (изображения, видео) с оптимизацией
- **Система тегов** для категоризации
- **MongoDB** для хранения данных
- **Защищенные маршруты** с middleware

### ✅ Admin Panel (React + TypeScript)
- **Страница входа** с валидацией
- **Dashboard** со статистикой
- **Список упражнений** с фильтрацией (все/опубликованные/черновики)
- **Rich Text редактор TipTap** с поддержкой:
  - Форматирование текста (Bold, Italic, Headings)
  - Списки (маркированные, нумерованные)
  - Изображения (адаптивные под ширину)
  - Ссылки
- **Drag & Drop** для загрузки медиа (react-dropzone)
- **Сортировка медиа** в карусели (dnd-kit)
- **Загрузка по URL** или файлом
- **Управление тегами** (создание, применение)
- **Публикация/снятие с публикации**

## 📦 Технологический стек

**Backend:**
- Node.js 18+
- Express 4
- TypeScript 5
- MongoDB + Mongoose
- JWT для аутентификации
- Multer + Sharp для медиа
- Bcrypt для паролей

**Frontend:**
- React 19
- TypeScript 5
- Vite 7
- React Router 6
- TipTap 2 (Rich Text Editor)
- dnd-kit (Drag & Drop)
- Axios (HTTP клиент)

## 🚀 Быстрый старт

```bash
# 1. Запустите MongoDB
brew services start mongodb-community

# 2. Установите зависимости (уже сделано)
npm install
cd admin-panel && npm install && cd ..

# 3. Инициализируйте БД
npm run seed

# 4. Запустите backend
npm run dev

# 5. В новом терминале - admin panel
cd admin-panel && npm run dev
```

**Доступ:**
- Backend API: http://localhost:5000
- Admin Panel: http://localhost:5173
- Login: seplitza@gmail.com / 1234back

## 📂 Структура файлов

```
Backend-rejuvena/
├── src/
│   ├── models/
│   │   ├── User.model.ts        # Модель пользователя
│   │   ├── Exercise.model.ts    # Модель упражнения
│   │   └── Tag.model.ts         # Модель тега
│   ├── routes/
│   │   ├── auth.routes.ts       # Аутентификация
│   │   ├── exercise.routes.ts   # CRUD упражнений
│   │   ├── media.routes.ts      # Загрузка медиа
│   │   └── tag.routes.ts        # Управление тегами
│   ├── middleware/
│   │   └── auth.middleware.ts   # JWT проверка
│   ├── scripts/
│   │   └── seed.ts              # Инициализация БД
│   └── server.ts                # Точка входа
├── admin-panel/
│   └── src/
│       ├── pages/
│       │   ├── Login.tsx        # Страница входа
│       │   ├── Dashboard.tsx    # Главная страница
│       │   ├── ExerciseList.tsx # Список упражнений
│       │   └── ExerciseEditor.tsx # Редактор упражнения
│       ├── components/
│       │   ├── Layout.tsx       # Layout с sidebar
│       │   └── TipTapEditor.tsx # Rich text редактор
│       ├── api/
│       │   └── client.ts        # Axios настройки
│       └── utils/
│           └── auth.ts          # Работа с токеном
├── uploads/                      # Загруженные файлы
├── .env                         # Конфигурация
├── package.json
├── QUICKSTART.md                # 👈 Инструкция для быстрого старта
├── SETUP.md                     # Подробная документация
└── DEPLOYMENT.md                # Развертывание на продакшн
```

## 🎨 Скриншоты функционала

### Упражнение (Exercise)
```
{
  "title": "Название упражнения",
  "description": "Краткое описание для списка",
  "content": "<h1>Заголовок</h1><p>Текст с <strong>форматированием</strong></p><img src='...' />",
  "carouselMedia": [
    {
      "url": "/uploads/image1.jpg",
      "type": "image",
      "filename": "image1.jpg",
      "order": 0
    }
  ],
  "tags": ["yoga-id", "beginner-id"],
  "isPublished": true
}
```

## 📡 API Examples

```javascript
// Логин
POST /api/auth/login
Body: { "email": "seplitza@gmail.com", "password": "1234back" }
Response: { "token": "jwt...", "user": {...} }

// Получить упражнения
GET /api/exercises
Headers: { "Authorization": "Bearer jwt..." }
Response: [{ Exercise }, ...]

// Создать упражнение
POST /api/exercises
Headers: { "Authorization": "Bearer jwt..." }
Body: { "title": "...", "description": "...", ... }

// Загрузить медиа
POST /api/media/upload
Headers: { "Authorization": "Bearer jwt..." }
Body: FormData with file
Response: { "url": "/uploads/...", "type": "image", ... }
```

## 🔧 Интеграция с фронтенд приложением Rejuvena

```javascript
// В вашем основном приложении Rejuvena
const API_URL = 'https://your-backend-url.com/api';

// Получить все опубликованные упражнения
const getExercises = async () => {
  const res = await fetch(`${API_URL}/exercises`);
  const exercises = await res.json();
  return exercises.filter(e => e.isPublished);
};

// Получить упражнение по ID
const getExercise = async (id) => {
  const res = await fetch(`${API_URL}/exercises/${id}`);
  return res.json();
};

// Получить все теги
const getTags = async () => {
  const res = await fetch(`${API_URL}/tags`);
  return res.json();
};
```

## ✨ Ключевые особенности

1. **TipTap редактор** - современный, расширяемый редактор с адаптивными медиа
2. **Drag & Drop** - для загрузки и сортировки изображений в карусели
3. **Загрузка по URL** - можно добавлять медиа не только файлами
4. **Оптимизация изображений** - автоматическая с помощью Sharp
5. **JWT аутентификация** - безопасная система входа
6. **Типизация TypeScript** - и на фронтенде, и на бэкенде
7. **Модульная архитектура** - легко расширять и поддерживать

## 🎯 Следующие шаги

1. **Запустите проект локально** (см. QUICKSTART.md)
2. **Создайте несколько упражнений** для тестирования
3. **Разверните на продакшн** (см. DEPLOYMENT.md):
   - Backend → Railway/Render
   - Admin Panel → GitHub Pages/Vercel
   - Database → MongoDB Atlas
4. **Интегрируйте с фронтенд** приложением Rejuvena

## 📚 Документация

- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт (начните здесь!)
- **[SETUP.md](SETUP.md)** - Подробная документация проекта
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Развертывание в продакшн
- **[README.md](README.md)** - Основная информация

## 🎉 Готово!

Проект полностью готов к использованию. Все зависимости установлены, структура создана, код написан.

**Чтобы начать работу:**
```bash
# 1. Запустите MongoDB
brew services start mongodb-community

# 2. Инициализируйте БД
npm run seed

# 3. Запустите backend и admin panel
npm run dev  # в корневой папке
cd admin-panel && npm run dev  # в другом терминале
```

Затем откройте http://localhost:5173 и войдите с учетными данными seplitza@gmail.com / 1234back

---

**Автор:** GitHub Copilot  
**Дата:** 3 января 2026  
**Версия:** 1.0.0
