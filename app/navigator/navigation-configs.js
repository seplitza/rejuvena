/**
 * Navigation custom configurations
 * @flow
 * @format
 */
import {CardStyleInterpolators} from '@react-navigation/stack';

// Slide animation as forHorizontalIOS
export const slideHorizontal = CardStyleInterpolators.forHorizontalIOS;
