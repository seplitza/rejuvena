# Unified Backend (Jan 16, 2026) - Strangler Fig Pattern
# All requests go to new backend, which has fallback to Azure for auth
# Using HTTPS domain to avoid mixed content issues on GitHub Pages
NEXT_PUBLIC_API_URL=https://api-rejuvena.duckdns.org
