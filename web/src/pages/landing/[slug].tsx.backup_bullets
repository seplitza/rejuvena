$(sed '223s/.*/              {landing.heroSection.subtitle.split("\\n").filter(line => line.trim()).map((line, idx) => {\n                const text = line.replace(\/^[-•]\\s*\/, "").trim();\n                if (!text) return null;\n                return (\n                  <li key={idx} className="text-left">\n                    {text}\n                  <\/li>\n                );\n              })}/' /Users/alexeipinaev/Documents/Rejuvena/web/src/pages/landing/[slug].tsx)
